librosa>=0.10.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
natsort>=8.2.0
demucs>=4.0.0
tqdm>=4.65.0
# ⚠ torch/torchvision/torchaudio は CUDA版を「先に」手動インストールすること:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU版の場合:
#   pip install torch torchvision torchaudio
# ※ demucs が PyPI の CPU版 torch を引き込むため、CUDA版を先にインストールしてから
#   pip install demucs --no-deps の順序で入れるのが安全